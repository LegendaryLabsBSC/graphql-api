import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { Document } from 'mongoose';

// export type RejuDocument = Reju & Document;
// export const RejuSchema = SchemaFactory.createForClass(Reju);
